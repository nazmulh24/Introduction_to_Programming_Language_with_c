#include <stdio.h>
int main()
{
    // int a, b, sum;
    // scanf("%d %d", &a, &b);
    // sum = a + b;
    // printf("Sum = %d\n", sum);

    // int a;
    // float b;
    // char c;
    // scanf("%d %f %c", &a, &b, &c);
    // printf("%d %.3f %c\n", a, b, c);

    int x, y;
    // scanf("%d%% %d%%", &x, &y);
    // printf("%d%% %d%% \n", x, y);

    // char p;
    // scanf("%d%c %d%c", &x, &p, &y, &p);
    // printf("%d%% %d%% \n", x, y);

    scanf("%da %db", &x, &y);
    printf("%da %db \n", x, y);

    return 0;
}
